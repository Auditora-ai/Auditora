# QA Report — 2026-04-02 Cycle 1

**Agent:** QA Agent #05  
**Scope:** Phase 1 code (Issues #2–#13), all existing tests, deep code path audit  
**Repo state:** All Phase 1 issues marked DONE

---

## 1. Test Results

| Suite | Passed | Failed | Total |
|-------|--------|--------|-------|
| process-engine (bpmn-layout-preprocessor) | 18 | 0 | 18 |
| process-engine (bpmn-layout-visual) | 2 | 0 | 2 |
| process-engine (bpmn-builder) | 28 | 0 | 28 ✅ **FIXED** (was 9/28) |
| **Total** | **48** | **0** | **48** |

**TypeScript (saas):** 6 errors
- `evaluaciones/page.tsx:91` — Duplicate JSX attribute `organizationSlug`
- `packages/database/prisma/client.ts:17,25,27,29,35` — `_ext` type mismatch on PrismaClient

**TypeScript (marketing):** ✅ Clean

**Build (saas):** ✅ Passes (Next.js tolerates TS errors at build time)  
**Build (marketing):** ✅ Passes

---

## 2. Bugs Found

| # | Severity | Component | Summary |
|---|----------|-----------|---------|
| BUG-001 | 🔴 Critical | `packages/api/modules/organizations/lib/membership.ts` | Auth bypass: `verifyOrganizationMembership` returns null but callers don't check |
| BUG-002 | 🔴 Critical | `apps/saas/app/api/organization/profile/route.ts` | Uses first org instead of active org (`orgs?.[0]?.id`) |
| BUG-003 | 🔴 Critical | `apps/saas/app/(unauthenticated)/forgot-password/` | Missing page route — 404 on forgot password |
| BUG-004 | 🟠 High | `apps/saas/.../evaluaciones/page.tsx:91` | Duplicate `organizationSlug` prop on `EvaluacionesTabs` |
| BUG-005 | 🟠 High | `apps/saas/app/api/scan/save/route.ts:52` | Unsafe `JSON.parse` with `as string` cast on riskResults |
| BUG-006 | 🟠 High | `apps/saas/modules/auth/components/SessionProvider.tsx` | `loaded` never true for unauthenticated users |
| BUG-007 | 🟠 High | `apps/saas/app/api/organization/profile/route.ts:37` | No input validation on PUT (arbitrary fields pass to DB) |
| BUG-008 | 🟠 High | `apps/saas/modules/onboarding/components/OnboardingSetupCompleteStep.tsx:47` | Wrong route `/settings/members` missing org slug prefix |
| BUG-009 | 🟠 High | `apps/saas/modules/onboarding/components/OnboardingAccountStep.tsx:23` | Missing `.min(1)` on name validation — empty name accepted |
| BUG-010 | 🟡 Medium | `apps/saas/.../onboarding/components/OnboardingCompanyStep.tsx` | 20+ hardcoded English strings (form labels, validation, select options) |
| BUG-011 | 🟡 Medium | `apps/saas/.../onboarding/components/OnboardingCompanyStep.tsx:84` | `companyName` collected but never sent to API |
| BUG-012 | 🟡 Medium | `packages/process-engine/lib/bpmn-builder.ts:58,81` | Hardcoded Spanish labels "Inicio"/"Fin" in BPMN output |
| BUG-013 | 🟡 Medium | `packages/payments/lib/usage.ts:50-81,119-153` | Sequential DB queries that could be parallelized |
| BUG-014 | 🟡 Medium | `apps/saas/.../deliverables/page.tsx:12` | `redirect()` inside `.then()` — flash of empty content |
| BUG-015 | 🟡 Medium | `apps/saas/modules/payments/components/UsageDashboard.tsx:120` | No Zod validation on API response |
| BUG-016 | 🟡 Medium | `apps/saas/modules/auth/components/LoginForm.tsx:280` | "Remember me" checkbox not connected to auth |
| BUG-017 | 🟡 Medium | `packages/database/prisma/client.ts:17` | `_ext` internal Prisma type used for global/client typing |
| BUG-018 | 🟢 Low | `apps/saas/.../evaluaciones/components/EvaluacionesTabs.tsx` | Missing ARIA tab pattern (role, aria-selected) |
| BUG-019 | 🟢 Low | `apps/saas/.../OnboardingForm.tsx:22` | Out-of-range step renders blank (no error feedback) |
| BUG-020 | 🟢 Low | `apps/saas/.../shared/components/NavBar.tsx:80-96` | Indentation corruption from merge refactoring |
| BUG-021 | 🟢 Low | `apps/saas/app/api/scan/save/route.ts:166` | Silent `.catch(() => {})` swallows errors |

**Total: 3 Critical, 6 High, 8 Medium, 4 Low**

---

## 3. Tests Fixed This Cycle

- **bpmn-builder.test.ts**: Fixed 19 failing tests
  - Made `buildBpmnXml` tests async (function now returns Promise)
  - Updated `dims("task")` expected width: 100 → 160
  - Added connection targets for orphaned-node tests (new filtering logic)
  - Result: 48/48 pass ✅

---

## 4. Code Quality Score

**Overall: 68/100**

| Category | Score | Notes |
|----------|-------|-------|
| Type Safety | 55/100 | 6 tsc errors, `any`-like Proxy type, `_ext` abuse |
| Error Handling | 60/100 | Auth bypasses, missing null checks, silent catches |
| Security | 55/100 | Auth bypass in membership check, no input validation on profile PUT |
| i18n | 40/100 | 20+ hardcoded English strings in onboarding, Spanish in BPMN |
| Accessibility | 50/100 | Missing ARIA patterns on tabs, icon-only buttons |
| Performance | 70/100 | Sequential DB queries, double org fetches |
| Test Coverage | 65/100 | 48 tests pass but no coverage for auth, scan, onboarding flows |

---

## 5. Top 3 Priorities for Agent #04

1. **🔴 BUG-001 + BUG-002 + BUG-003** — Fix the 3 Critical bugs immediately:
   - Add null check after `verifyOrganizationMembership()` (auth bypass)
   - Fix `getOrgId()` to use active org, not first org
   - Create forgot-password page route

2. **🟠 BUG-004 through BUG-009** — Fix High severity bugs:
   - Remove duplicate prop on EvaluacionesTabs
   - Safe JSON.parse on riskResults
   - Fix SessionProvider loaded state
   - Add Zod validation on profile PUT
   - Fix onboarding completion route
   - Add .min(1) to name validation

3. **🟡 i18n Sprint** — Onboarding company step needs full i18n (BUG-010, BUG-012). 20+ hardcoded strings will block LATAM expansion.

---

## 6. Recommendations for Agent #03

- **Block Fase 2 start** until 3 Critical bugs are fixed — auth bypass is a showstopper for any paid feature
- **Prioritize F2-01 (Scan free tier)** after Critical fixes — scan save has unsafe JSON.parse (BUG-005)
- **i18n debt is accumulating** — onboarding wizard (BUG-010) and BPMN builder (BUG-012) both have hardcoded strings. Suggest a dedicated i18n cleanup issue.
- **Test coverage is thin** — auth, scan, and onboarding have zero tests. Recommend adding smoke tests before Fase 2.
